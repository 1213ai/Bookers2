<body>
<h2>Users</h2>
<table class="table">
          <thead>
           <tr>
            <!--↓画像を入れる-->
             <th></th>
             <th>image</th>
             <th>name</th>
             <th colspan="3"></th>
           </tr>
         </thead>

        <tbody>
        <% @users.each do |user| %>
           <tr>
            <!--↓画像を入れる-->
             <td></td>
             <%= link_to user_path(user.id) do %>
             <td><%= user.name %></td>
             <% end %>
             <td><%= link_to "Show", user_path(user.id), class: "link"  %></td>

           </tr>
         </tbody>

          <% end %>
        </table>

</body>







